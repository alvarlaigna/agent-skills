build/
cmake-build-*/
*.o
*.exe

# Web/Emscripten build output is emitted at the project root. Anchor these to the
# root so the source web shell in web/ (web/minshell.html) stays tracked.
/*.html
/*.js
/*.wasm
/*.data
